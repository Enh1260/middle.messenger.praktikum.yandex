.search-list__wrap
	ul.search-list
		if users
			each user in users
				li.search-item
					input(type='checkbox' name="user_id" value=user.id)
					| #{user.first_name} #{user.second_name} #{ user.display_name } 
